---
trigger: always_on
---
- save your work iteratively using commits
- make sure to run `uv run pyright` to check for type errors before submitting work for human review
- run `uv run src/main.py --replay --no-voice --duration 5` to check for runtime errors. DO NOT DO THIS for small code changes, be pragmatic and efficient
- use `uv run` to run python scripts

